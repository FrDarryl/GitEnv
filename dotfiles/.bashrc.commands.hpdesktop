#!/bin/bash

mgd()
{
    read -p "Google account ID: " accountId
    [[ -n ${accountId} ]] && google-drive-ocamlfuse -label ${accountId} ${HOME}/gdrive/${accountId}
}
alias mwin10="sudo mount -o ro /dev/sda2 /media/frdarryl/win10"
pwg()
{
    grep -i "$@" ${HOME}/gdrive/revdjordan/Personal/passwd.json
}
